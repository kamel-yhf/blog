{% extends 'base.html.twig' %}

{% block title %}Hello BlogController!
{% endblock %}

{% block body %}
<section class="articles">

{% for article in articles %}
        <article>
            <div class="content">
            <div class="meradata">Ecrit le {{article.createdAt | date('d/m/Y')}} à {{ article.createdAt | date('H:i')}}</div>
            <h2>{{ article.title }}</h2>
                <img src={{ article.image }} alt="">
                <p>{{ article.content }}</p>

                <a href="{{path('blog_show', {'id' : article.id})}}" class="btn btn-info">Lire la suite</a>
            </div>
        </article>
        <br>
{% endfor %}
</section>


{% endblock %}
